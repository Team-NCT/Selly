import { DescCardList } from "@/components/common";
import { args } from "./dummy";

const Explore = () => {
  return (
    <main>
      Explore
      <DescCardList {...args} />
    </main>
  );
};

export default Explore;
