import { CardProps } from "./Card.types";
import style from "./Card.module.scss";

const Card = () => {
  return (
    <section>
      <h1>좀비와 함께 춤을</h1>
      <figure></figure>
    </section>
  );
};

export default Card;
