export interface ImageProps {
  setIsImageTrue: (a: boolean) => void;
}

export interface TitleProps {
  setIsTitleTrue: (a: boolean) => void;
}

export interface LinkProps {
  setIsLinkTrue: (a: boolean) => void;
}
